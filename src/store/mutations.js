export const setPlayed = (state, payload) => {
  state.played = payload
}

export const setVersion = (state, payload) => {
  state.version = payload
}

export const setStartingDate = (state, payload) => {
  state.setStartingDate = payload
}
