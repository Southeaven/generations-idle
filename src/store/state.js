const engineRunning = false
const engineInterval = null
const played = false
const version = '0.1.0'
const startingDate = new Date()

export default {
  engineRunning,
  engineInterval,
  played,
  version,
  startingDate
}
